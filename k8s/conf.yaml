apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-auth-config
  namespace: default
data:
  SERVICE_NAME: todo-auth-service
  SERVICE_PORT: "6000"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-auth-deployment
  labels:
    app: todo
    component: todo-auth
spec:
  replicas: 3
  selector:
    matchLabels:
      component: todo-auth
  template:
    metadata:
      labels:
        component: todo-auth
    spec:
      containers:
        - name: todo-auth-pod
          image: dreambo8563docker/todo-auth:v4
          ports:
            - containerPort: 7000
          volumeMounts:
            - mountPath: /k8slog
              name: k8slog
          livenessProbe:
            httpGet:
              path: /healthz
              port: 7000
            initialDelaySeconds: 3
            periodSeconds: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 7000
            initialDelaySeconds: 3
            periodSeconds: 3
      volumes:
        - name: k8slog
          hostPath:
            path: /Users/vincent/Documents/Golang/logs
            type: DirectoryOrCreate
---
kind: Service
apiVersion: v1
metadata:
  name: todo-auth-service
spec:
  type: NodePort
  selector:
    component: todo-auth
  ports:
    - protocol: TCP
      nodePort: 30000
      port: 6000
      targetPort: 7000
